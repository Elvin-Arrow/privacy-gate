# Dependencies
node_modules/
npm-debug.log
package-lock.json
.env.local
.env.*.local

# Build outputs
dist/
target/
*.o
*.a
*.so
*.dylib
*.exe
.DS_Store

# Cargo
Cargo.lock

# cargo-mutants output (testing.md §5)
mutants.out/

# Vite
.vite

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.env

# Environment-specific
.env.local
.env.*.local

# OS
Thumbs.db
Desktop.ini

# Detector ONNX weights (architecture §4.2). Nightly/release may vendor them;
# PRs may skip (testing.md). Pin lives in core/src/detector/hybrid.rs
# (`NER_PII_ONNX_SHA256`) and models/README.md — never fetched at runtime.
models/*.onnx
